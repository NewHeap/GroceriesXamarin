<?xml version="1.0" encoding="UTF-8"?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" x:Class="GroceriesPlatformApp.Views.AddGroceriePage" Title="New Product">
    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Save" Clicked="Save_Clicked" />
    </ContentPage.ToolbarItems>
    <ContentPage.Content>
        <StackLayout Spacing="20" Padding="15">
            <Label Text="Product" FontSize="Small" />
            <Entry Text="{Binding Groceries.Product}" FontSize="Small" />
            <Label Text="Stock" FontSize="Small" />
            <Entry Keyboard="Numeric" Text="{Binding Groceries.Stock}" FontSize="Small" Margin="0" />
            <Label Text="BuyLocation" FontSize="Small" />
            <Entry Keyboard="Numeric" Text="{Binding Groceries.BuyLocation}" FontSize="Small" Margin="0" />
            <Label Text="StoreName" FontSize="Small" />
            <Entry Keyboard="Numeric" Text="{Binding Groceries.StoreName}" FontSize="Small" Margin="0" />
        </StackLayout>
    </ContentPage.Content>
</ContentPage>
